# python=3.10                  # Streamlit Cloudで3.10推奨（wheelエラー回避）
streamlit>=1.28.0,<1.40.0      # メインのStreamlit本体（範囲指定で最新安定版）
pandas>=2.0.0,<2.3.0           # データ処理
numpy>=1.24.0,<2.0.0           # 数値計算
pdfplumber>=0.10.0             # PDF読み込みに必須
streamlit-option-menu>=0.3.6   # サイドメニューなどに使用
openpyxl>=3.0.0                # Excel出力がある場合に備えて（pd.ExcelWriterで使用）
